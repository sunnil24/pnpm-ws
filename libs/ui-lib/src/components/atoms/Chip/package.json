{
  "name": "@xyzproject/chip",
  "version": "0.2.0",
  "description": "Chip component",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist",
    "README.md",
    "package.json"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "dependencies": {
    "@radix-ui/themes": "^2.x",
    "@xyzproject/tailwind-config": "workspace:*",
    "class-variance-authority": "0.x"
  },
  "peerDependencies": {
    "react": "18.x",
    "react-dom": "18.x"
  },
  "devDependencies": {
    "@testing-library/react": "14.x",
    "@testing-library/jest-dom": "6.x",
    "@storybook/react": "7.x",
    "vitest": "^1.x"
  },
  "scripts": {
    "publish": "pnpm publish",
    "test": "vitest ./",
    "lint": "eslint ./",
    "lint:fix": "eslint '**/*.{ts,tsx}' --fix",
    "build": "tsup",
    "clean": "rm -rf ./dist ./.turbo",
    "cleanmodules": "rm -rf ./node_modules"
  }
}
