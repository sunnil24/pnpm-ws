import { Canvas, Meta } from "@storybook/addon-docs";
import Chip from "./index";
import * as ChipStories from "./Chip.stories";
import { ArgTypes } from "@storybook/blocks";

<Meta of={ChipStories} />
`
# Chip

A badge-like component which dsiplays the text with a coloured background.

## Props

<ArgTypes of={Chip} />

## Usage

### Default

<Canvas of={ChipStories.Default} />

### Status Variants

<Canvas of={ChipStories.StatusVariants} />