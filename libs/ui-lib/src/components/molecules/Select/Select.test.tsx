// // Dropdown.spec.tsx
// import { render, screen, fireEvent } from "@testing-library/react";
// import { describe, test, expect } from "vitest";
// import "@testing-library/jest-dom/vitest";
// import Select from "./Select";

// describe("Select", () => {
//   const options = ["Apples", "Bananas", "Oranges"];

//   test("renders correctly and displays options", () => {
//     console.log(render(<Select options={options} />));

//     // Verify that the dropdown trigger button is rendered
//     const triggerButton = screen.getByText("Apples");
//     expect(triggerButton).toBeInTheDocument();

//     const optionElement = screen.getByText("Apples");
//     expect(optionElement).toBeInTheDocument();
//   });

// });
