import { defineConfig } from "tsup";

export default defineConfig({
  clean: true,
  entry: {
    index: "index.ts",
  },
  format: ["esm", "cjs"],
  minify: true,
  outDir: "dist",
  dts: true,
  sourcemap: false,
  treeshake: true,
  splitting: true,
});
