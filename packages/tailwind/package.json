{
  "name": "@xyzproject/tailwind-config-design-system",
  "version": "1.0.5",
  "type": "module",
  "devDependencies": {
    "tsup": "7.2.0",
    "copyfiles": "2.4.1"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "files": [
    "dist",
    "package.json"
  ],
  "dependencies": {
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.2.0"
  },
  "scripts": {
    "tw-build":"tsup && copyfiles themes/* config/* styles/* dist",
    "tw-pub": "npm publish",
    "linktw": "cd dist && npm link",
    "tw-drypub": "npm publish --dry-run"
  }
}
